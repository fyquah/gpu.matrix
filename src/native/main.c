// standard libraries
#include <assert.h>
#include "tests/test_vector.h"
#include "utils.h"
#include "ndarray.h"
#include "vector.h"

int main() {
    gpu_matrix_init();
    ndarray * arr = malloc(sizeof(arr));

    // test_vector();
    return 0;
}
